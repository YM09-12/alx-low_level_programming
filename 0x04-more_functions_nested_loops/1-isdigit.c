#include "main.h"

/**
 * _isdigit - write function that check for a digit (0 through 9).
 * @c: charto check
 * Return: 0 or 1
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (0);
else
return (0);
}
